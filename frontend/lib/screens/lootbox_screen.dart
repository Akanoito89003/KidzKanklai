import 'package:flutter/material.dart';

class LootboxScreen extends StatelessWidget {
  const LootboxScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Lootbox Fashion')),
      body: const Center(child: Text('Lootbox Page')),
    );
  }
}
