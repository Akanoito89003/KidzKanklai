# 📱 KidzKanklai Frontend

โปรเจกต์นี้เป็นส่วนหน้าบ้าน (Frontend) ของแอปพลิเคชัน KidzKanklai พัฒนาด้วย **Flutter**

> [!CAUTION]
> **⚠️ อ่านก่อนเริ่มเขียนโค้ด! (Disclaimer)**
> ไฟล์ต่างๆ ที่อยู่ในโฟลเดอร์ `lib/screens/` ขณะนี้ เป็นเพียง **Template หรือโครงร่างตัวอย่าง** เท่านั้น
> สิ่งที่คุณเห็นอยู่ **ไม่ใช่หน้าจอเกมที่สมบูรณ์** คุณมีหน้าที่ต้อง:
> 1.  **แก้ไขหน้า UI**: ให้ตรงกับ Design จริง (Figma/Adobe XD)
> 2.  **เขียน Logic**: การทำงานจริงของแต่ละหน้าให้สมบูรณ์
> *ตัวอย่างเช่น:* `lobby_screen.dart` คือไฟล์สำหรับหน้า **Lobby (Home)** คุณต้องแก้ให้เป็นหน้าหลักของเกมจริงๆ

## 📁 คู่มือโครงสร้างโฟลเดอร์ (Directory Structure)

เพื่อให้เข้าใจว่าไฟล์ไหนคืออะไร และอันไหนลบได้/ไม่ได้:

| ชื่อโฟลเดอร์/ไฟล์ | หน้าที่ (What is it?) | ลบได้ไหม? (Safe to delete?) |
| :--- | :--- | :--- |
| **`android/`** | เก็บโค้ด Native ภาษา Kotlin/Java ของ Android เอาไว้ตั้งค่า Icon, Permission | ❌ **ห้ามลบเด็ดขาด** (ถ้าลบ แอปจะรันบน Android ไม่ได้) |
| **`ios/`** | เก็บโค้ด Native ภาษา Swift/Obj-C ของ iOS เอาไว้ตั้งค่า Info.plist | ❌ **ห้ามลบเด็ดขาด** (ถ้าลบ แอปจะรันบน iOS ไม่ได้) |
| **`lib/`** | **หัวใจของแอป** เก็บโค้ดภาษา Dart ที่เราเขียนทั้งหมด | ❌ **ห้ามลบ** |
| **`build/`** | เก็บไฟล์ขยะที่เกิดจากการ Compiling (เช่น apk) | ✅ **ลบได้** (เดี๋ยว Flutter สร้างใหม่เองเมื่อกด Run) |
| **`.dart_tool/`** | เก็บ Cache ของ dependencies | ✅ **ลบได้** (ถ้าเจอปัญหาแปลกๆ ลบโฟลเดอร์นี้ช่วยได้) |
| **`.idea/`** | เก็บการตั้งค่าของโปรแกรม Editor (เช่น VS Code/Android Studio) | ⚠️ **ไม่ควรลบ** (ถ้าลบ การตั้งค่าโปรเจกต์จะหาย ต้องตั้งใหม่) |
| **`pubspec.yaml`** | ใบรายการของที่ต้องซื้อ (Dependencies Manager) บอกว่าแอปชื่ออะไร ใช้ Library อะไรบ้าง | ❌ **ห้ามลบเด็ดขาด** |

## 🛠️ สิ่งที่ต้องมีก่อนเริ่ม (Prerequisites)

1.  **Flutter SDK**: เวอร์ชัน 3.10.0 ขึ้นไป (ตรวจสอบด้วยคำสั่ง `flutter doctor`)
2.  **Code Editor**: แนะนำ **VS Code** (พร้อมลง Extension: Flutter) หรือ Android Studio
3.  **Backend**: ต้องรัน Backend Server (Go) ให้ทำงานก่อน (ดูวิธีรันในโฟลเดอร์ `backend`)

## 🚀 วิธีติดตั้งและรันโปรเจกต์ (Installation & Running)

### 1. ติดตั้ง Library ต่างๆ
เปิด Terminal เข้าไปที่โฟลเดอร์ `frontend` แล้วพิมพ์คำสั่ง:
```bash
flutter pub get
```

### 2. ตรวจสอบการตั้งค่า Server IP ⚠️ (สำคัญมาก)
เปิดไฟล์ `lib/api_service.dart` และตรวจสอบบรรทัดที่ `baseUrl`:

*   **กรณีใช้ Android Emulator:**
    ```dart
    static const String baseUrl = "http://10.0.2.2:8080";
    ```
*   **กรณีใช้ iOS Simulator / Web / Windows:**
    ```dart
    static const String baseUrl = "http://localhost:8080";
    ```
*   **กรณีใช้มือถือ Android จริง (เสียบสาย):**
    ต้องแก้เป็น IP ของเครื่องคอมพิวเตอร์ (ตรวจสอบด้วย `ipconfig`) เช่น:
    ```dart
    static const String baseUrl = "http://192.168.1.xxx:8080";
    ```

### 3. รันแอปพลิเคชัน
พิมพ์คำสั่งด้านล่างเพื่อรันแอป:
```bash
flutter run
```
หรือกดปุ่ม **Run** (Play) ใน VS Code

## 📁 โครงสร้างโฟลเดอร์สำคัญใน `lib/`
*   `lib/main.dart`: จุดเริ่มต้นของแอปและการจัดการ Route (เส้นทางเดินรถของแอป)
*   `lib/api_service.dart`: ตัวจัดการการเชื่อมต่อกับ Backend และ Model ข้อมูล
*   `lib/screens/` -> หน้าจอต่างๆ ของแอป
    *   `lobby_screen.dart` -> หน้า Lobby / Home
    *   `login_screen.dart` -> หน้า Log In
    *   `fashion_screen.dart` -> หน้าแต่งตัว
    *(และไฟล์อื่นๆ ให้แก้ตามชื่อไฟล์เลย)*

## 🎨 วิธีเพิ่มรูปภาพ/โลโก้ (How to add Images/Assets)

หากต้องการนำรูปภาพ (เช่น โลโก้, พื้นหลัง) มาใส่ในแอป ให้ทำตามขั้นตอนนี้:

1.  **สร้างโฟลเดอร์**:
    *   สร้างโฟลเดอร์ชื่อ `assets` ไว้ที่ root ของโปรเจกต์ (ระดับเดียวกับ `lib`, `android`)
    *   สร้างโฟลเดอร์ย่อยข้างใน เช่น `assets/images/`

2.  **ใส่ไฟล์รูป**:
    *   เอารูป `.png` หรือ `.jpg` ไปวาง (เช่น `logo.png`)

3.  **ตั้งค่าใน `pubspec.yaml`**:
    *   เปิดไฟล์ `pubspec.yaml`
    *   หาหัวข้อ `flutter:` (บรรทัดล่างๆ)
    *   เพิ่มบรรทัด `assets:` เข้าไป (ระวังย่อหน้าต้องตรงกัน!)
    ```yaml
    flutter:
      uses-material-design: true
      assets:
        - assets/images/
    ```

4.  **วิธีเรียกใช้ในโค้ด**:
    ```dart
    // แสดงรูปภาพ
    Image.asset('assets/images/logo.png', width: 100, height: 100)

    // หรือใช้เป็น DecorationImage (พื้นหลัง)
    DecorationImage(
      image: AssetImage('assets/images/background.jpg'),
      fit: BoxFit.cover,
    )
    ```

## 📝 สิ่งที่ต้องทำต่อ (ToDo List สำหรับคนรับงาน)

1.  **เปลี่ยนรูปภาพและไอคอน**:
    *   ตอนนี้ในโค้ดใช้รูปจาก Internet (NetworkImage) หรือ Icon พื้นฐาน
    *   **สิ่งที่ต้องทำ**: สร้างโฟลเดอร์ `assets/images/` แล้วเอารูปจริงไปใส่ จากนั้นแก้โค้ดให้ใช้ `Image.asset()` แทน
    *   อย่าลืมไปลงทะเบียนโฟลเดอร์ใน `pubspec.yaml` ด้วยนะ:
        ```yaml
        assets:
          - assets/images/
        ```

2.  **เชื่อมต่อ API จริง**:
    *   ตอนนี้บางปุ่ม (เช่น Google Login) ยังเป็นแค่ Mockup
    *   **สิ่งที่ต้องทำ**: เขียนฟังก์ชันใน `api_service.dart` เพิ่มเติมเพื่อรองรับฟีเจอร์อื่นๆ เช่น ลืมรหัสผ่าน, ซื้อของ, รับเควส

3.  **ตกแต่ง UI (Styling)**:
    *   หน้าจอตอนนี้เป็นโครงร่างคร่าวๆ
    *   **สิ่งที่ต้องทำ**: ปรับสี, ฟอนต์, และระยะห่าง (Padding/Margin) ให้ตรงกับ Design System ของเกม

## ❓ ปัญหาที่พบบ่อย (Troubleshooting)
*   **Login ไม่ได้ / หมุนติ้วๆ**:
    *   เช็คว่ารัน Backend หรือยัง?
    *   เช็คว่าตั้งค่า IP ใน `api_service.dart` ถูกต้องกับเครื่องที่ใช้รันไหม? (Emulator vs Real Device)
    *   ถ้าใช้ Android เครื่องจริง มือถือกับคอมต้องต่อ WiFi วงเดียวกัน
*   **Error: uses-material-design**: ลองรัน `flutter clean` แล้วตามด้วย `flutter pub get` อีกครั้ง
*   **หาโฟลเดอร์ `build`, `.dart_tool`, `.idea`, `.vscode` ไม่เจอ?**:
    *   ไม่ต้องตกใจ! GitHub จะไม่เก็บโฟลเดอร์พวกนี้ไว้
    *   **วิธีแก้**:
        *   `build`, `.dart_tool`: กดรันคำสั่ง `flutter run` เดี๋ยวมาเอง
        *   `.idea`, `.vscode`: เดี๋ยวโปรแกรม Editor (VS Code / Android Studio) จะสร้างให้เองเมื่อเปิดโปรเจกต์ครับ
