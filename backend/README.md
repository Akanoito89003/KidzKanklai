# ⚙️ KidzKanklai Backend

โปรเจกต์นี้เป็นส่วนหลังบ้าน (Backend API) ของแอปพลิเคชัน KidzKanklai พัฒนาด้วย **Go (Golang)** ทำหน้าที่เชื่อมต่อฐานข้อมูลและจัดการ Logic เกม

## 🛠️ สิ่งที่ต้องมีก่อนเริ่ม (Prerequisites)

1.  **Go Language**: เวอร์ชัน 1.23.0 หรือใหม่กว่า (ดาวน์โหลดที่ [golang.org](https://golang.org/dl/))
2.  **PostgreSQL**: เวอร์ชัน 15 หรือ 16
3.  **Code Editor**: แนะนำ **VS Code** (พร้อมลง Extension: Go)

## 🗄️ การติดตั้งและเชื่อมต่อฐานข้อมูล (Database Setup)

โปรเจกต์นี้ใช้ **PostgreSQL** เป็นฐานข้อมูล คุณต้องทำการติดตั้งและสร้าง Database บนเครื่องของคุณก่อน

### 1. ติดตั้ง PostgreSQL
ถ้ายังไม่มี ให้ติดตั้งก่อน:
*   **Windows**: ดาวน์โหลด Installer จาก [postgresql.org](https://www.postgresql.org/download/windows/)
*   **Mac**: ใช้ `brew install postgresql`

### 2. สร้าง Database
เข้าไปที่ pgAdmin หรือใช้คำสั่ง SQL สร้าง Database ชื่อ `kidzkanklai` (หรือชื่ออื่นตามต้องการ)

---

---

## 🚀 การเตรียม Deploy (เชื่อมต่อกับ Supabase Database)

เมื่อจะนำ Backend ขึ้น Server จริง (Deploy) 

### วิธีเปลี่ยนไปใช้ Supabase:

1.  **เตรียม Database**:
    *   ไปที่ [supabase.com](https://supabase.com) สร้างโปรเจกต์ใหม่
    *   ไปที่ `Settings` (ไอคอนเฟือง) -> `Database`
    *   หาหัวข้อ **Connection string** -> เลือกแถบ `URI` หรือ `Golang`
    *   Copy ข้อความมา

2.  **แก้ไขโค้ดใน `main.go`**:
    *   หาบรรทัด `connStr` แล้วเปลี่ยนเป็น Link ของ Supabase
    *   **ตัวอย่าง:**
    ```go
    // แบบ Local (เดิม)
    // connStr = "postgresql://postgres:pass@localhost:5432/kidzkanklai?sslmode=disable"
    
    // แบบ Supabase (ของใหม่สำหรับ Deploy)
    connStr = "postgresql://postgres.xxxx:[YOUR-PASSWORD]@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require"
    ```

> **ข้อควรระวัง:**
> *   อย่าลืมใส่ **Password** ของ Supabase ลงไปแทนที่ `[YOUR-PASSWORD]`
> *   ต้องเติม `?sslmode=require` ต่อท้าย URL เสมอ เพื่อความปลอดภัย

---

### 3. ตั้งค่าการเชื่อมต่อในโค้ด ⚠️ (สำคัญมาก)
เปิดไฟล์ **`main.go`** และค้นหาบรรทัดที่กำหนดตัวแปร `connStr`:

```go
// ไฟล์ main.go บรรทัดประมาณ 69
connStr = "postgresql://postgres:password123@localhost:5432/kidzkanklai?sslmode=disable"
```

คุณต้องแก้ข้อความใน `""` ให้ตรงกับเครื่องของคุณ:
*   `postgres`: ชื่อผู้ใช้ (User)
*   `password123`: รหัสผ่าน (Password) ที่คุณตั้งตอนลง Postgres
*   `localhost`: โฮสต์ (ถ้าลงเครื่องตัวเองใช้ localhost ถูกแล้ว)
*   `5432`: พอร์ต (ค่าเริ่มต้นคือ 5432)
*   `kidzkanklai`: ชื่อ Database ที่คุณสร้างไว้

> **Tips:** ถ้าไม่อยากแก้โค้ด สามารถตั้งเป็น Environment Variable ชื่อ `DATABASE_URL` แทนได้

## 🚀 วิธีรัน Server (How to Run)

1.  เปิด Terminal เข้าไปที่โฟลเดอร์ `backend`
2.  พิมพ์คำสั่งเพื่อดาวน์โหลด Library ที่ต้องใช้:
    ```bash
    go mod tidy
    ```
3.  สั่งรัน Backend:
    ```bash
    go run main.go
    ```

ถ้าสำเร็จ หน้าจอจะขึ้นว่า:
```
✅ Connected to Database!
✅ Database tables checked/created.
🚀 Server is running on port 8080
```

## 📁 โครงสร้างไฟล์สำคัญ (Project Structure)
มีการแยกโค้ดออกเป็นส่วนๆ เพื่อให้ง่ายต่อการแก้ไข:

*   **`main.go`**: ไฟล์หลักสำหรับ **เริ่มรันโปรแกรม** (Entry Point)
*   **`config/database.go`**: ตั้งค่าการ **เชื่อมต่อ Database** และสร้างตาราง (Migration)
*   **`models/models.go`**: เก็บโครงสร้างข้อมูล (**Struct**) เช่น User, Item
*   **`handlers/handlers.go`**: เก็บ **Logic หลัก** ของระบบ (เช่น ฟังก์ชันล็อกอิน, สมัครสมาชิก)
*   **`routes/routes.go`**: กำหนด **URL Path** ต่างๆ (Router) ว่าลิงก์ไหนไปเรียกฟังก์ชันอะไร

> **💡 อยากแก้ไขอะไร?**
> *   *อยากแก้ตาราง DB?* -> ไปที่ `config/database.go`
> *   *อยากแก้ Logic สมัครสมาชิก?* -> ไปที่ `handlers/handlers.go`
> *   *อยากเพิ่ม URL ใหม่?* -> ไปที่ `routes/routes.go`
*   **`go.mod` / `go.sum`**: ไฟล์จัดการ Library (คล้าย package.json ของ JS)

## 📡 API Endpoints ที่มีอยู่
*   `POST /register`: สมัครสมาชิก
*   `POST /login`: เข้าสู่ระบบ
*   `GET /me/:id`: ดูข้อมูลโปรไฟล์
*   `POST /equip`: เปลี่ยนไอเทมสวมใส่
*   `POST /me/update`: อัปเดตข้อมูลส่วนตัว

---
106: **หมายเหตุ:** เมื่อรันครั้งแรก ระบบจะสร้าง **User ทดสอบ** ให้โดยอัตโนมัติ (Email: `test@example.com`, Pass: `password123`) ลองเอาไปล็อกอินได้เลย!
107: 
108: ## ❓ คำถามที่พบบ่อย (FAQ)
109: 
110: *   **หาไฟล์ `.exe` หรือตัวเข้าเกมไม่เจอ?**:
111:     *   ปกติครับ! เพราะเราส่งมอบแค่ **Source Code**
112:     *   เมื่อคุณรันคำสั่ง `go run main.go` ระบบจะทำการสร้างโปรแกรมและรันให้ทันที (โดยไม่ทิ้งไฟล์ `.exe` ไว้ให้รกเครื่อง)
113:     *   ⚠️ **ข้อควรระวัง:** ห้ามนำไฟล์ `.exe` หรือไฟล์ Binary ที่เกิดจากการ Build ขึ้น GitHub เด็ดขาด (เพราะใช้ข้ามเครื่องไม่ได้)
