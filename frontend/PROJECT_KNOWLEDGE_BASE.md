# ฐานข้อมูลความรู้โปรเจกต์ KidzKanklai
*รวบรวมรายการแก้ไขและการปรับจูนประสิทธิภาพ (กุมภาพันธ์ 2026)*

เอกสารนี้รวบรวมวิธีการแก้ปัญหา เทคนิคการเพิ่มความเร็ว และสถานะล่าสุดของระบบตัวละคร (Rive Character) ในแอปพลิเคชันนี้

---

## 1. การแก้ไขบั๊กสำคัญ (Critical Fixes)

### ✅ ปัญหาเปลี่ยนทรงผมไม่ได้ (Hair Input Not Found)
- **อาการ:** โค้ดหาตัวแปร 'HairID' ไม่เจอ ทำให้เปลี่ยนทรงผมแล้วไม่เกิดอะไรขึ้น
- **วิธีแก้:** ใช้ระบบ **"Manual Search"** (วนหาด้วยตัวเอง)
  - เปลี่ยนจากการใช้คำสั่ง `findInput` แบบปกติ มาเป็นการวนลูปเช็ครายการ Input ทั้งหมดใน Controller เพื่อหาตัวที่มีชื่อว่า 'HairID' โดยตรง ทำให้มีความเสถียร 100%

### ✅ ปัญหาจอดำ / ค้างตอนเปิดแอป (Black Screen / Freezing)
- **อาการ:** เปิดแอปแล้วจอดำ หรือค้างไปประมาณ 2-5 วินาที
- **สาเหตุ:** การโหลดไฟล์ Rive ขนาดใหญ่ (5MB) แบบสดๆ บน Main Thread ทำให้เครื่องหยุดประมวลผลกราฟิกชั่วคราว
- **วิธีแก้:**
  1. เพิ่มระบบ **Asynchronous Pre-caching** (แอบโหลดไฟล์รอตั้งแต่วินาทีแรกที่เปิดแอป)
  2. เพิ่มคำสั่ง `await RiveFile.initialize();` เพื่อเตรียมระบบกราฟิกให้พร้อมก่อนโหลดไฟล์จริง เพื่อป้องกันการสะดุด

---

## 2. การเพิ่มประสิทธิภาพ (Performance Optimization)

### 🚀 ระบบโหลดล่วงหน้า (Global Pre-Caching)
- **สิ่งที่ทำ:** สร้างคลาส `RiveCache` (`utils/rive_cache.dart`)
- **การทำงาน:** ทันทีที่เปิดแอป (`main.dart`) ระบบจะโหลดไฟล์ตัวละครเก็บเข้าหน่วยความจำทันที
- **ผลลัพธ์:** ทำให้หน้า `LobbyScreen` และ `FashionScreen` แสดงตัวละครได้ **ทันที (Instant)** โดยไม่ต้องเสียเวลาโหลดไฟล์ใหม่ทุกครั้งที่สลับหน้า

### 📉 การลดขนาดไฟล์รูปภาพ (Image Optimization)
- **สิ่งที่ทำ:** ลดขนาดไฟล์ Rive จาก **5MB -> 1.5MB**
- **เทคนิค:**
  - ย่อรูปภาพในโปรแกรม Rive (ผ่านแถบ Assets)
  - ตัดขอบ Artboard ให้ชิดตัวละคร (Close crop) เพื่อลดพื้นที่ว่างที่ไม่จำเป็น

### 🖥️ หมายเหตุเรื่อง Emulator
- **ข้อควรทราบ:** หากรันบน Emulator อาจจะยังเห็นอาการกระตุก (`Skipped Frames`) อยู่บ้าง ซึ่งเป็นเรื่องปกติเพราะ Emulator ประมวลผลกราฟิกแบบ Vector ได้ช้ากว่ามือถือจริงมาก (บนเครื่องจริงจะลื่นไหลปกติ)

---

## 3. สถานะและเครื่องมือ (Dev Tools)

### 🛠️ โหมดข้ามหน้า Login (Dev Mode)
- **สถานะ:** 🟢 **เปิดใช้งานอยู่ (ENABLED)**
- **ผลลัพธ์:** เปิดแอปแล้วเด้งเข้าหน้า `LobbyScreen` ทันที (ข้ามหน้ารหัสผ่าน)
- **ข้อดี:** ช่วยให้แก้ไข UI และ Animation ได้ไวขึ้นโดยไม่ต้องรอ Backend
- **วิธีปิด:** แก้ไฟล์ `main.dart` เปลี่ยน `initialRoute: '/lobby'` กลับเป็น `'/login'`

### 🐞 คู่มือการ Debug
- **Log:** ระบบจะพ่น Log ออกมาทาง Terminal โดยค้นหาคำว่า "Rive Debug" หรือ "FashionScreen" เพื่อดูค่าสถานะต่างๆ ได้

---

## 4. คู่มือการอัปเดต (How-To)

### 🔄 วิธีอัปเดตโมเดลตัวใหม่
หากคุณแก้ไขโมเดลใน Rive เสร็จแล้ว:
1.  **Export:** เซฟไฟล์นามสกุล `.riv` ออกมา
2.  **ตั้งชื่อ:** ตั้งชื่อให้ตรงกับในโค้ด (ปัจจุบันคือ **`Model1103.riv`**)
3.  **วางทับ:** ลากไฟล์ไปวางทับในโฟลเดอร์ `frontend/assets/animation/`
4.  **Restart:** กดปิดแอปแล้วเปิดใหม่ (Cold Boot) เพื่อเคลียร์ Cache เก่า

### 📐 การปรับขนาดตัวละคร
- **ตำแหน่งโค้ด:** ปรับแก้ที่ `Container` (ความสูง/กว้าง) ในไฟล์ `LobbyScreen.dart` และ `FashionScreen.dart`
- **ค่าปัจจุบัน:**
  - **Lobby:** สูง 500, กว้าง 400
  - **Fashion:** สูง 280, กว้าง 300 (ลดลงเพื่อไม่ให้บังเมนู)

---
*รวบรวมและแปลโดย Antigravity*
